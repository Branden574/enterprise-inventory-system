[phases.setup]
nixPkgs = ['nodejs', 'npm']

[phases.install]
cmds = ['cd backend && npm ci --production']

[phases.build]
cmds = ['cd backend && echo "Build complete"']

[start]
cmd = 'cd backend && npm start'
